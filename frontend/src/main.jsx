import React from "react";
import ReactDOM from "react-dom/client";
import { ReleaseNotesApp } from "./ReleaseNotesApp";
import "./release-notes.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ReleaseNotesApp />
  </React.StrictMode>
);
